# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Projection cache (removed — PCA is live, t-SNE/UMAP dropped)
backend/cache/

# Per-user dataset sessions (runtime state: recipes + uploaded polytopes)
backend/sessions/

# Python venv (local backend testing only)
backend/.venv/

# OS / editor
.DS_Store
.vscode/
*.log

# Duplicate data files — canonical copies live in data/
/polytope.npz
/polytope_samples.npz

# Samples are generated from the polytope at build time — no samples file shipped.
backend/data/polytope_samples_*.npz

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Ad-hoc screenshots (not needed to run the project)
/Screenshot 2026-06-06 115145.png
